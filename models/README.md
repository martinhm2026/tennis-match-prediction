# Trained Models

This directory contains serialized classification models and the
preprocessing scaler used by the web application.

The models must only be published if they were trained using data that
can be legally and ethically shared.
